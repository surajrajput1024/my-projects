export const CORS = {
  allowedOrigins: ['*'],
  methods: ['GET', 'POST', 'DELETE', 'PATCH', 'PUT'],
};

export enum DeviceType {
  ANDROID = 'android',
  IOS = 'ios',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHERS = 'others',
}
